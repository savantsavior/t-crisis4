You must build the game executable using the following packages:

Make, G++, SDL2+dev, SDL2_Image+dev, SDL2_Mixer+dev, SDL2_TTF+dev

Once above packages are installed through a package manager
open console and type: "make"

If game bulds correctly there will be a new file in the game's folder
